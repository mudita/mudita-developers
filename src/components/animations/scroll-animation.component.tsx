import React from "react"
import AppFunctionComponent from "../../types/app-function-component.interface"

const ScrollAnimation: AppFunctionComponent = () => {
  return <div>Scroll Animation</div>
}

export default ScrollAnimation
